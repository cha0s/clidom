{
  "name": "clidom",
  "version": "0.1.2",
  "description": "Parse a DOM from the command line",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "clidom": "bin/clidom"
  },
  "author": "cha0s",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.19.0",
    "css-what": "^2.0.2",
    "request": "^2.53.0",
    "yargs": "^3.5.4"
  }
}
